const showedNumbersForPagination = (arr,currentPage)=>{
    const index = currentPage-1;
    
    if(currentPage > 2){
        return [arr[index-1],currentPage,arr[index+1]];
    }
    return slice
 }